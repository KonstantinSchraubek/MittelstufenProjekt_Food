import {ListRecipe} from './list-recipe';


describe('List-Recipe', () => {
  it('should create an instance', () => {
    expect(new ListRecipe('test', 'test', 'test', 'test')).toBeTruthy();
  });
});
